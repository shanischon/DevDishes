apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devdishes.fullname" . }}-test-connection"
  labels:
    {{- include "devdishes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devdishes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
